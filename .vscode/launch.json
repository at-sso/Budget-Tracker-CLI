{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Default",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
        }
    ]
}